[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "quantalogic-stub"
version = "0.1.0"
description = "Minimal quantalogic stub"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
